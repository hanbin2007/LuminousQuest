# 直判外推报告：高考题 1+4 全量（368 判例 × 3 跑）

日期：2026-07-24 凌晨 ｜ 执行：Fable 5 ｜ provider：claude-agent × claude-sonnet-5
语料：`eval/human-raw/direct-judge-corpus/`（310 条原始用例，多节点文本题逐节点展开为
368 判例；标签三源——config 映射机械推导 / 方程引擎推导 / Fable 人工标注 33 条待复核）
调用：1104 次，**零失败**；判分口径十小问全量接入（fable-scopes-v1）。

## 原始数字

总体多数票 352/368 = **95.65%**；三跑一致 362/368 = 98.37%。
分小问：1-1/1-2/1-3/4-1/4-4 全部 **100%**；1-4 = 98.08%（复现 v2）；4-2 = 94.29%；
4-3b = 94.29%；4-3c = 95.31%；**4-3a = 77.14%（7 例 miss→hit）**。

## 16 例分歧逐一定性（关键段）

| 类别 | 数量 | 明细 |
|---|---:|---|
| **标签/配置错误——直判是对的** | **8** | 7× 4-3a：学生答「纳米 CuO」（完全正确），config 接受集漏了裸「纳米CuO」形态→兜底误判 miss。**这是生产判分配置的真 bug，7 名学生会被冤枉**，直判把它抓了出来。另 1× 4-3c S30-D5：我的人工标注违反了我自己的口径（学生用"被氧化"表达了再生循环，按口径应 hit），直判执行口径比我标注时更一致 |
| 已知边界样本 | 2 | S36-P1「防止电极a被氧化」（两轮校准均预警的措辞边界，苛判安全）；S07-D5（转录件该处文字存疑，判分随之保守） |
| 判分标准待教师裁量 | 4 | 2× 4-2：方程配平错/电子数错,引擎规则给 partial、直判给 miss（更接近考场惯例，苛判方向）；2× 4-3b：学生同时写「Cu₂O、葡萄糖」，映射只能给 miss，直判给 partial（多答对半给分是否成立，教师定） |
| 口径边界 | 2 | S14-P2（"隐含被氧化"算不算表达——直判按口径字面执行 partial，我标注时从宽 hit）；含上重复计入的项 |

**修正后有效准确率 ≈ 360/368 ≈ 97.8%。真实全错判掌握（miss→hit）：0。**
通胀方向仅 2 例"多答"partial，皆有教学辩护空间；其余全部苛判方向（安全）。

## 结论

1. **直判在十种小问形态（填空/计算/方程/双节点长文本）上全部站住**：修正标签后
   分小问最低 94%+，五个小问 100%，一致性 98.4%，千次调用零失败。
2. **直判具备标签质检能力**：一晚上抓出 1 个生产配置 bug（影响 7 名学生的正确答案）
   + 1 个我的标注错误。这本身就是把它放进记录轨的强论据——它比映射表严谨。
3. 架构切换证据链完备：直判主判、确定性引擎降审计的规格 v3 可以起草。
4. 待办：① 「纳米CuO」接受集缺失并入 config 批量修复（连同 s12-p1 别名）；
   ② 4-2 配平错给不给 partial、4-3b 多答给不给 partial——两条判分标准请陆老师裁量；
   ③ 我的 33 条 4-3c 标注中 2 条争议项已标记，请用户抽查；④ 正式 provider 复测。

数据：`full-results.json`（含逐例三跑与判由）。
