# 任务计划（2026-07-24 起）

状态基线：M6 Phase 1/2/2.5/3 已入库；直判证据链闭环（52 例 98.08% + 368 例修正后
≈97.8%、真实全错判掌握 0、具备标签质检能力）；人性化判分（槽位级降级）已上线；
sync 幂等/版本徽标/超时修复已上线。日历：8/8 功能冻结、8/12 RC、8/15 交陆老师、
9 月初断网彩排×3。

## A. 主线：直判上位（规格 v3）

1. **[已完成] 规格 v3 起草**：文本题直判为主判进记录轨（按题灰度：膜题与
   题 1/题 4 十小问先行，其余题验一道上一道）；抽取管线降为静默审计轨（分歧留痕
   教师，不拦学生）；判分口径成为一等配置资产（每题一份，可由 evidence/rubric
   机械组装 + 人工润色）。评审照规矩双路，实现 codex max。
2. **[代码完成；正式录制受 E.1 阻塞] 直判生产化**：v2/外推的提示词模板与口径
   装配器进 server 正式代码；录音/回放/demo 语义与既有 cacheKey 机制对齐；审计轨
   分歧事件复用 divergence 通道。
3. **[阻塞：E.1] 正式 provider 复测**：一致性/延迟基线，替换 claude-agent
   开发通道。

## B. Config 批量修复（一次做完，随做随重录）

- 「纳米CuO」进 4-3a 接受集（外推实验实锤的生产 bug，影响 7 名学生）
- s12-p1 别名缺口（K₂O 深层论证同义表述）
- 方程式形态别名（「Zn − 2e⁻」等价「Zn 失电子」——审计轨落地率）
- valueDomain/hint 推广到训练案例判断槽（审计轨健壮性）
- 完成后：configDigest 全链路重同步（tests/fixtures + recordings sed +
  generate-demo-class-sessions + font:subset）+ **demo 全量重录**（含
  `pnpm record:agent-session` 采录 agent 会话）+ 全量回归

## C. 陆老师裁量清单（下次见面一次带齐）

1. K⁺-only 理由 → P1 记分（现 miss，建议 partial；涉 6 名学生）
2. 方程配平错/电子数错 → 给不给 partial（引擎规则 vs 考场惯例，涉 4-2）
3. 「Cu₂O、葡萄糖」式多答 → 给不给半分（涉 4-3b）
4. 直判上记录轨的知情确认（拿外推报告讲）

## D. 用户抽查/决策

1. 4-3c 33 条 Fable 标注抽查（`pending-user-review`，2 条争议已标记）
2. 规格 v3 放行（见 A.1）
3. stash@{0}（合并前端前的并行 WIP）处置

## E. 外部依赖（用户驱动）

1. 生产 LLM 通道：Modelverse 充值或其他国内 key（赛场机器无 Claude 订阅）
2. 缺页照片 5 人（王品成/邱佳桐/庄子墨/张松肇/薛恩欣）+ S20 的 1-3 当面问
3. 语料扩标：其余题目的真实作答批注（直判外推到全部题目的前提，也是 M1c
   语料门槛 150/5/3 的路径）

## F. M6 收尾

1. Track D 视觉精修（真浏览器调手感）：聊天气泡/题卡/总结卡层级、滚动锚定、
   pending/fallback 状态区分、教师分歧红点样式、focus 镜头平滑趋近
2. 断网 demo 端到端彩排一次（录制→回放零网络，版本不符拒启动已生效）
3. **M6 里程碑终审**：gpt-5.6-sol max + fable-5 双报告并过（规矩不变）

## G. 发布链

1. dist/release 重冻结（8/12 RC 前；含版本徽标、槽位级降级、agent loop 全量）
2. 全量测试 + 打包 + LAN 模式验证为冻结门禁

优先级序：A（用户放行后立即）> B > F > C/D 并行 > G；E 持续催办。
