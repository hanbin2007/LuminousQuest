# Eval Reports

Each CLI mode writes `latest-<mode>.md` here. Generated latest reports are
ignored by Git. Reports contain configured gates, per-node accuracy, the
five-way confusion matrix, severe false-mastery denominator, citation/schema
rates, consistency, metamorphic invariance, latency, tokens, and estimated cost
when pricing is configured.
